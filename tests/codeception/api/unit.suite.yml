# Codeception Test Suite Configuration

# suite for unit (internal) tests.
# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.

class_name: UnitTester
modules:
  enabled:
    - Asserts
coverage:
  enabled: true
  remote: false
  whitelist:
    include:
      - ../../../api/modules/api/v1/services/*