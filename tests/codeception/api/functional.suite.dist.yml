# after clone adjust your urls
# and add to git ignore!
modules:
  config:
    PhpBrowser:
      url: 'http://genesis-test.loc'
    REST:
      depends: PhpBrowser
      url: 'http://genesis-test.loc/api'
