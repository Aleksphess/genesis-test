# after clone adjust your urls
# and add to git ignore!
modules:
  config:
    PhpBrowser:
      url: 'http://genesis-test.loc:8080'
    REST:
      depends: PhpBrowser
      url: 'http://genesis-test.loc:8080/api'
